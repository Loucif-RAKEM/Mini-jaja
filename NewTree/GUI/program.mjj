class test {
    final int longueur;
    boolean glob;
    int w;  
    main {
        int x;
        boolean y = true || false;
        int z = 1+x;
        boolean test;
        test=(true && true) || false;

        x=0;

        longueur=((5 + 6 -3)*x)/longueur;
        glob=true && test;
        w=(7 - 4 + 9) / (13 * x);
    }
}
