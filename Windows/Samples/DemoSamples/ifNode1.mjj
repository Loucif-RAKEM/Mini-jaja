class test {
int test(int a,int b){
	return a*b;
};
	main {
		int x = 2;
		int y = 0;
		int z;
		if(x>0 && y > x){ 
			x=3;
			y=5;
			z=x+y;
		}; 
		if(x==5){ 
			if(!(y > 4 && z == 2)){
				x = x - 1;
			}	  
		}; 
		if(x==0 && (z*y > 3) ){ 
				x = y - z;  
		}; 
		if(!(x==0)){ 
				  
		}; 
		test (1,2);
	}
}
