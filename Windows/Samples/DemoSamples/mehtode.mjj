class test{
    int fact(int a, int b){
        return a*b;
    };
    
    void increment(int x){
    	if(x > 5){
    		increment(x+1);
    	};
    	return x;
    };
    
    main{
    
    int x = fact(1,2);
    increment(1);
    }
}
