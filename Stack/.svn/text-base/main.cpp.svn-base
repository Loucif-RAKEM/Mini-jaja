#include <iostream>
#include "Quadruplet.h"
#include "Stack.h"

int main(int argc, char* argv[]){
	/*Quadruplet q("toto");
	Quadruplet q1("jack");
	Stack s;	
	s.push(q);
	s.push("toto");
	s.top().display();
	s.push(q1);
	s.top().display();

	s.pop();
	s.top().display();
	s.pop();*/
//	Stack p;

	
	return 0;
}
