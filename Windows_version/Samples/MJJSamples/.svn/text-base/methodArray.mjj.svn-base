class testtttest{
    int firstMeth(int x, int y) {
        return x * y;
    };
    
    int secondMeth(int x) {
        int x;
    };
    
    main{
        int tab[5];
        int x = 0;
        int y;
        while(!x>5) {
            tab[x] = secondMeth(x);
            x++;
        };
        firstMeth(3,5);
        y = tab[2] + tab[4] + firstMeth(tab[1], tab[3]);
        tab[0] = y;
    }
}
